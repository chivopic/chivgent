/** Per-deployment overrides. Production is stricter than staging. */
export const PRODUCTION_TIMEOUT_MS = 30000;
export const STAGING_TIMEOUT_MS = 15000;
export const DEVELOPMENT_TIMEOUT_MS = 60000;
